({
	receiveJobs : function(component, event, helper) {
        helper.receiveJobs(component, event);
	},
    runTests : function(component, event, helper) {
        helper.runTests(component, event);
    }
})